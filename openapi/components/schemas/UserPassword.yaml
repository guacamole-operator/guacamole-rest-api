type: object
properties:
  newPassword:
    type: string
    format: password
    writeOnly: true
  oldPassword:
    type: string
    format: password
    writeOnly: true
required:
  - newPassword
  - oldPassword
